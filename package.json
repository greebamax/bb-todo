{
  "private":true,
  "dependencies": {},
  "devDependencies": {
    "eslint": "^3.12.2",
    "eslint-config-airbnb-base": "^11.0.0",
    "eslint-plugin-import": "^1.16.0",
    "json-server": "^0.9.4"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "json": "./node_modules/.bin/json-server -p 8080 db.json",
    "http": "./node_modules/.bin/http-server -p 3000",
    "dev": "./node_modules/.bin/json-server -p 3000 db.json --static ./ --middlewares delay.js"
  }
}
