{
  "private":true,
  "dependencies": {},
  "dependencies": {
    "rollup-plugin-node-resolve": "^3.0.0"
  },
  "devDependencies": {
    "eslint": "^3.12.2",
    "eslint-config-airbnb-base": "^11.1.3",
    "eslint-plugin-import": "^2.2.0",
    "json-server": "^0.10.0",
    "rollup": "^0.41.6",
    "rollup-watch": "^3.2.2"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "json": "./node_modules/.bin/json-server -p 8080 db.json",
    "http": "./node_modules/.bin/http-server -p 3000",
    "dev": "./node_modules/.bin/json-server -p 3000 db.json --static ./build --middlewares delay.js",
    "build": "./node_modules/.bin/rollup --config rollup.config.js"
  }
}
